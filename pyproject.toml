[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "dnadis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "intervaltree",
]

[project.scripts]
dnadis = "dnadis.cli:main"

[tool.setuptools.packages.find]
include = ["dnadis*"]
