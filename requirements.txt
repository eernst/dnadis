# Core dependencies for final_finalizer
# Install with: pip install -r requirements.txt

# Required for TOML config file support (Python < 3.11)
tomli>=2.0.0

# Required for O(n log n) overlap detection (recommended)
intervaltree>=3.1.0
